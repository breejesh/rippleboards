export const environment = {
  production: false,
  // Set this to your Firebase Realtime Database REST endpoint (no trailing slash),
  // e.g. https://your-project-id-default-rtdb.firebaseio.com
  firebaseDatabaseUrl: '',
  // Optional: Firebase Cloud Functions base URL for simulation endpoints
  firebaseFunctionsUrl: '',
  // Optional: Firestore project id (if using Firestore instead of Realtime DB)
  firestoreProjectId: 'rippleboard-sdoh'
};
